{
  "name": "mdmc-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "audit:responsive": "node scripts/audit-responsive.mjs --widths 390,768",
    "gate": "node scripts/url-gate.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "wrangler": "^4.140.0"
  }
}
